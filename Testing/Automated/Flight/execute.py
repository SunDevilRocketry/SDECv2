import time
# Execute

# This test doesn't have to do anything! The firmware side handles everything
# we need. The script only exists so the framework is happy.

time.sleep(10) # Allow enough time for calib to complete
# Also -- ensure that the LD refresh time exceeds this!